name: Example GitHub Action

on:
  push:
    branches:
      - main  # Trigger the action on push to the main branch

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: 22f1000913@ds.study.iitm.ac.in
        run: echo "Hello, world!"
